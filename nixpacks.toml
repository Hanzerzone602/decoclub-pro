[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "gcc", "zlib"]

[phases.install]
cmds = ["npm install --omit=dev", "python3 -m pip install --user -r requirements.txt"]

[start]
cmd = "node server.js"
